warn The configuration property `package.json#prisma` is deprecated and will be removed in Prisma 7. Please migrate to a Prisma config file (e.g., `prisma.config.ts`).
For more information, see: https://pris.ly/prisma-config

Environment variables loaded from .env
Prisma schema loaded from prisma\schema.prisma
Datasource "db": PostgreSQL database "finance_app", schema "public" at "localhost:5432"

Drift detected: Your database schema is not in sync with your migration history.

The following is a summary of the differences between the expected database schema given your migrations files, and the actual schema of the database.

It should be understood as the set of changes to get from the expected schema to the actual schema.

If you are running this the first time on an existing database, please make sure to read this documentation page:
https://www.prisma.io/docs/guides/database/developing-with-prisma-migrate/troubleshooting-development

[+] Added enums
  - AccountType
  - AuditAction
  - DocumentType
  - TransactionStatus
  - TransactionType
  - WorkspaceRole
  - WorkspaceType

[+] Added tables
  - Account
  - AccountVerificationToken
  - AuditLog
  - Category
  - Notification
  - PasswordResetToken
  - RefreshToken
  - Transaction
  - User
  - Workspace
  - WorkspaceMember

[*] Changed the `Account` table
  [+] Added index on columns (workspaceId)
  [+] Added foreign key on columns (workspaceId)

[*] Changed the `AccountVerificationToken` table
  [+] Added index on columns (userId)
  [+] Added foreign key on columns (userId)

[*] Changed the `AuditLog` table
  [+] Added index on columns (entity, entityId)
  [+] Added index on columns (userId)
  [+] Added foreign key on columns (userId)

[*] Changed the `Category` table
  [+] Added index on columns (workspaceId)
  [+] Added foreign key on columns (workspaceId)

[*] Changed the `Notification` table
  [+] Added index on columns (userId)
  [+] Added foreign key on columns (userId)

[*] Changed the `PasswordResetToken` table
  [+] Added index on columns (userId)
  [+] Added foreign key on columns (userId)

[*] Changed the `RefreshToken` table
  [+] Added index on columns (userId)
  [+] Added foreign key on columns (userId)

[*] Changed the `Transaction` table
  [+] Added index on columns (accountId)
  [+] Added index on columns (workspaceId, attachmentSize)
  [+] Added index on columns (workspaceId, date)
  [+] Added unique index on columns (workspaceId, fitid)
  [+] Added unique index on columns (workspaceId, hashDeduplication)
  [+] Added foreign key on columns (accountId)
  [+] Added foreign key on columns (categoryId)
  [+] Added foreign key on columns (workspaceId)

[*] Changed the `User` table
  [+] Added unique index on columns (cpf)
  [+] Added unique index on columns (email)

[*] Changed the `WorkspaceMember` table
  [+] Added index on columns (userId)
  [+] Added unique index on columns (userId, workspaceId)
  [+] Added index on columns (workspaceId)
  [+] Added foreign key on columns (userId)
  [+] Added foreign key on columns (workspaceId)

We need to reset the "public" schema at "localhost:5432"

You may use prisma migrate reset to drop the development database.
All data will be lost.
