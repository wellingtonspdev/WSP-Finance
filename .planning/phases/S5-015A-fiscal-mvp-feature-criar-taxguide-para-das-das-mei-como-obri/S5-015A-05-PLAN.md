---
phase: S5-015A
plan: 05
type: tdd
wave: 5
depends_on:
  - S5-015A-04
files_modified:
  - backend/src/services/TaxGuideService.ts
  - backend/tests/services/TaxGuideAudit.test.ts
  - backend/tests/services/AuditLogService.test.ts
autonomous: false
requirements:
  - TG-01
  - TG-02
  - TG-03
  - TG-04
  - TG-05
  - TG-06
  - TG-07
  - TG-08
  - TG-09
  - TG-10
  - TG-11
must_haves:
  truths:
    - "Audit metadata for create/upload/paid/cancel is allowlisted."
    - "AuditLog contains no raw PDF, raw payload, PII, original filename, objectKey, storage path, bucket, or provider URL."
    - "Focused TaxGuide backend tests cover every TG-01..TG-11 success criterion."
    - "Backend-only scope is preserved; frontend diff is empty."
  artifacts:
    - path: "backend/tests/services/TaxGuideAudit.test.ts"
      provides: "TaxGuide-specific audit safety tests"
    - path: "backend/tests/services/AuditLogService.test.ts"
      provides: "Shared audit regression coverage if helper changes are needed"
    - path: "backend/src/services/TaxGuideService.ts"
      provides: "TaxGuide audit metadata allowlist helper if required"
  key_links:
    - from: "backend/src/services/TaxGuideService.ts"
      to: "backend/src/services/AuditLogService.ts"
      via: "AuditLogService.logSync safe metadata"
      pattern: "logSync"
    - from: "backend/tests/services/TaxGuideAudit.test.ts"
      to: "S5-015A-VALIDATION.md"
      via: "acceptance sweep mapping"
      pattern: "TG-11"
---

<objective>
Lock TaxGuide audit safety and run the backend-only acceptance sweep across schema, service, routes, uploads, tenant isolation, and file metadata safety.

Purpose: close LGPD/RBAC/RLS acceptance risk before phase verification and prevent sensitive fiscal file data from entering AuditLog.
Output: audit tests, optional TaxGuide audit helper, and focused validation evidence.
</objective>

<execution_context>
@C:/Users/Wellington/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Wellington/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-CONTEXT.md
@.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-RESEARCH.md
@.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-VALIDATION.md
@backend/src/services/AuditLogService.ts
@backend/tests/services/AuditLogService.test.ts

<interfaces>
Resolved audit decision:
- Do not include objectKey in AuditLog. Although TaxGuide stores `guideFileObjectKey` and `paymentProofObjectKey`, audit metadata uses only safe hash/flags, action, taxGuideId, workspaceId, file type, old/new status, and paidTransactionId when needed.
- Forbidden serialized audit substrings include `workspaces/`, `bucket`, `r2://`, `s3://`, `%PDF`, base64 PDF markers, original filenames, CPF/CNPJ-like values, emails, user names, raw request bodies, and object-key fields.
</interfaces>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Safe fiscal obligation backend with files and transaction link. | Plans 01-05; Plan 05 acceptance sweep |
| REQ | TG-01..TG-11 all success criteria. | Plan 05 validates all |
| RESEARCH | Audit uses hash/flags only, no objectKey. | Plan 05 |
| CONTEXT | D-02, D-06, D-07, D-08, D-09, D-10, D-12, D-13, D-14, D-15, D-16, D-17. | Plan 05 |
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED TaxGuide audit allowlist tests</name>
  <files>backend/tests/services/TaxGuideAudit.test.ts, backend/tests/services/AuditLogService.test.ts</files>
  <behavior>
    - Create audit includes action, taxGuideId, workspaceId, type, competence, status, and safe amount/date metadata if consistent with existing audit patterns.
    - Guide PDF upload audit includes file kind and safe hash/flags, not original filename, objectKey, object path, bucket, or raw PDF.
    - Payment proof upload audit follows the same allowlist.
    - Mark-paid audit includes old/new status and paidTransactionId, not full transaction.
    - Cancel audit includes old/new status only.
    - Serialized audit payload rejects forbidden substrings and sensitive shapes.
  </behavior>
  <action>Create TaxGuide audit tests or extend the nearest audit test file if local conventions prefer it. The tests must serialize `oldState`, `newState`, and metadata to assert forbidden content is absent. Do not loosen existing AuditLogService tests.</action>
  <verify>
    <automated>cd backend; pnpm test -- tests/services/TaxGuideAudit.test.ts tests/services/AuditLogService.test.ts</automated>
  </verify>
  <done>RED audit tests exist and fail only where TaxGuide audit metadata is missing or unsafe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement audit allowlist and run focused acceptance sweep</name>
  <files>backend/src/services/TaxGuideService.ts</files>
  <behavior>
    - TaxGuide audit metadata is built from an allowlist.
    - Upload audit uses hash/flags only and excludes objectKey.
    - Forbidden paid/cancel attempts do not create mutation audit rows.
    - Full focused TaxGuide validation passes.
  </behavior>
  <action>Add a TaxGuide-specific audit metadata helper inside `TaxGuideService` or a small local helper only if needed. Keep it narrow and do not refactor AuditLogService broadly. Run the S5-015A validation sweep from `S5-015A-VALIDATION.md`; fix only failures caused by S5-015A files. Do not touch frontend, Telegram/OCR, export internals, or Transaction mutation behavior. Do not stage, commit, push, reset, or clean.</action>
  <verify>
    <automated>cd backend; pnpm exec prisma validate</automated>
    <automated>cd backend; pnpm exec tsc --noEmit</automated>
    <automated>cd backend; pnpm test -- tests/integration/TaxGuideRLS.integration.test.ts tests/services/TaxGuideService.test.ts tests/services/TaxGuideUpload.test.ts tests/services/TaxGuideAudit.test.ts tests/routes/TaxGuide.route.test.ts</automated>
    <automated>git diff --check</automated>
    <automated>git diff -- frontend</automated>
  </verify>
  <done>Audit tests and full focused TaxGuide sweep pass, frontend diff is empty, and no stage/commit/push was performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Service mutation to AuditLog | Audit records are durable and must not receive raw or sensitive TaxGuide payloads. |
| Phase implementation to acceptance sweep | Validation must distinguish S5-015A failures from unrelated dirty worktree state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-S5-015A-05-01 | Information Disclosure | AuditLog oldState/newState | mitigate | Serialize audit payloads and forbid raw PDF, PII, filenames, objectKey, storage paths, buckets, provider URLs, and full request bodies. |
| T-S5-015A-05-02 | Repudiation | paid/cancel audit | mitigate | Audit successful OWNER paid/cancel mutations with old/new status and taxGuideId. |
| T-S5-015A-05-03 | Tampering | acceptance sweep | mitigate | Run schema, typecheck, focused tests, diff check, and frontend-diff proof before summary. |
| T-S5-015A-05-SC | Tampering | npm/pip/cargo installs | accept | No package installation is planned. |
</threat_model>

<verification>
<automated>cd backend; pnpm exec prisma validate</automated>
<automated>cd backend; pnpm exec tsc --noEmit</automated>
<automated>cd backend; pnpm test -- tests/integration/TaxGuideRLS.integration.test.ts tests/services/TaxGuideService.test.ts tests/services/TaxGuideUpload.test.ts tests/services/TaxGuideAudit.test.ts tests/routes/TaxGuide.route.test.ts</automated>
<automated>git branch --show-current</automated>
<automated>git status --short -uall</automated>
<automated>git diff --stat</automated>
<automated>git diff --check</automated>
<automated>git diff -- frontend</automated>
</verification>

<success_criteria>
Plan 05 succeeds when TaxGuide audit metadata is allowlisted, all TG-01..TG-11 validations are mapped and run, no sensitive data is logged, and backend-only scope remains proven.
</success_criteria>

<output>
Create `.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-05-SUMMARY.md` when done.
</output>
