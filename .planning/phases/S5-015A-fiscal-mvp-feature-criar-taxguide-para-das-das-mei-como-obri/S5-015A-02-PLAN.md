---
phase: S5-015A
plan: 02
type: tdd
wave: 2
depends_on:
  - S5-015A-01
files_modified:
  - backend/src/repositories/TaxGuideRepository.ts
  - backend/src/services/TaxGuideService.ts
  - backend/tests/services/TaxGuideService.test.ts
autonomous: false
requirements:
  - TG-01
  - TG-02
  - TG-03
  - TG-05
  - TG-07
  - TG-08
must_haves:
  truths:
    - "BUSINESS OWNER can create DAS and DAS_MEI TaxGuides with stored status PENDING."
    - "PERSONAL workspace TaxGuide actions return 403 Forbidden per D-02."
    - "ACCOUNTANT can create guides but cannot mark paid, link a transaction, cancel, or create a transaction per D-10."
    - "Projected response status is returned as `status`; MVP does not expose dual stored/projected status fields."
    - "Mark-paid requires an existing same-workspace effective paid Transaction with `isPaid = true`, `status = COMPLETED`, and `type = EXPENSE`."
  artifacts:
    - path: "backend/src/repositories/TaxGuideRepository.ts"
      provides: "Workspace-scoped TaxGuide persistence wrapper"
    - path: "backend/src/services/TaxGuideService.ts"
      provides: "BUSINESS-only domain rules, filters, overdue projection, payment link validation, cancel rules"
    - path: "backend/tests/services/TaxGuideService.test.ts"
      provides: "Focused service TDD coverage for rules and invariants"
  key_links:
    - from: "backend/src/services/TaxGuideService.ts"
      to: "backend/src/repositories/TaxGuideRepository.ts"
      via: "workspace-scoped repository methods"
      pattern: "workspaceId"
    - from: "backend/src/services/TaxGuideService.ts"
      to: "prisma.transaction"
      via: "read-only same-workspace paid EXPENSE validation"
      pattern: "type.*EXPENSE"
    - from: "backend/src/services/TaxGuideService.ts"
      to: "TaxGuide DTO"
      via: "projected status mapping"
      pattern: "status"
---

<objective>
Implement the TaxGuide domain service and repository rules without exposing HTTP routes or upload bytes.

Purpose: enforce BUSINESS-only fiscal obligation behavior, role permissions, overdue projection, and read-only transaction linking before controllers are wired.
Output: service, repository, and focused service tests.
</objective>

<execution_context>
@C:/Users/Wellington/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Wellington/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-CONTEXT.md
@.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-RESEARCH.md
@.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-VALIDATION.md
@backend/src/lib/prisma.ts
@backend/src/services/TransactionService.ts
@backend/src/repositories/TransactionRepository.ts

<interfaces>
Resolved research decisions for this plan:
- Effective payment validation is not open: linked `Transaction` must be same-workspace, `isPaid = true`, `status = COMPLETED`, and `type = EXPENSE`; the current schema has `Transaction.type` and `EXPENSE`.
- API/service DTOs return projected `status` as the only status field in MVP. Do not expose `storedStatus`, `projectedStatus`, or dual status fields.
- TaxGuide service must not call `TransactionService.create`, transaction update methods, account balance updates, or any ledger mutation path.
</interfaces>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | TaxGuide financial obligation management with optional link to existing Transaction. | Plan 02 service rules |
| REQ | TG-01 Create DAS succeeds. | Plan 02 |
| REQ | TG-02 Create DAS_MEI succeeds. | Plan 02 |
| REQ | TG-03 PERSONAL workspace returns 403. | Plan 02 |
| REQ | TG-05 ACCOUNTANT creates but cannot mark paid. | Plan 02 |
| REQ | TG-07 OVERDUE correct after dueDate passes. | Plan 02 |
| REQ | TG-08 Transaction link respects workspace/effective payment. | Plan 02 |
| RESEARCH | Payment transaction must be same-workspace, effective paid, and EXPENSE. | Plan 02 |
| RESEARCH | Return projected `status` only. | Plan 02 |
| CONTEXT | D-02, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-15, D-16. | Plan 02 |
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED service tests for TaxGuide domain rules</name>
  <files>backend/tests/services/TaxGuideService.test.ts</files>
  <behavior>
    - BUSINESS OWNER creates DAS and receives PENDING.
    - BUSINESS ACCOUNTANT creates DAS_MEI and receives PENDING.
    - PERSONAL workspace create/list/get/paid/cancel actions return 403.
    - Duplicate workspace/type/competence maps to conflict, preferably 409.
    - List filters by status, competenceMonth, and competenceYear.
    - Past-due PENDING guide returns DTO `status = OVERDUE` while PAID/CANCELLED do not.
    - Mark paid requires paidTransactionId.
    - Mark paid rejects cross-workspace, unpaid, non-COMPLETED, and non-EXPENSE transactions.
    - Mark paid accepts same-workspace `isPaid = true`, `status = COMPLETED`, `type = EXPENSE`.
    - ACCOUNTANT cannot mark paid or cancel; VIEWER cannot mutate.
    - TaxGuide service never calls Transaction create/update/balance APIs.
  </behavior>
  <action>Create focused tests first. Mock or seed only what is needed to prove service rules. The tests must assert the DTO exposes one `status` field with projected value; do not test for `storedStatus` or `projectedStatus`. Confirm RED failure is missing TaxGuide service/repository implementation, not syntax.</action>
  <verify>
    <automated>cd backend; pnpm test -- tests/services/TaxGuideService.test.ts</automated>
  </verify>
  <done>RED service tests exist for all service rules and fail for missing implementation only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement workspace-scoped repository and service rules</name>
  <files>backend/src/repositories/TaxGuideRepository.ts, backend/src/services/TaxGuideService.ts</files>
  <behavior>
    - Repository lookups include `workspaceId`; no unscoped guide id lookup feeds service behavior.
    - Service rejects non-BUSINESS workspaces before TaxGuide mutation.
    - Service applies OWNER/ACCOUNTANT/VIEWER action rules.
    - Payment link validation is read-only and requires same workspace, `isPaid = true`, `status = COMPLETED`, `type = EXPENSE`.
    - Get/list map stored PENDING past due date to DTO `status = OVERDUE`.
  </behavior>
  <action>Create `TaxGuideRepository` with focused create/find/list/update methods that accept optional transaction clients where useful and always scope by numeric `workspaceId`. Create `TaxGuideService` using the RLS-aware `prisma` import, Decimal-compatible persisted amounts, explicit caller role/action context, and status projection mapping. Do not use `sysPrisma`, `managementClient`, frontend files, upload storage calls, or any Transaction mutation path.</action>
  <verify>
    <automated>cd backend; pnpm exec prisma validate</automated>
    <automated>cd backend; pnpm exec tsc --noEmit</automated>
    <automated>cd backend; pnpm test -- tests/services/TaxGuideService.test.ts</automated>
  </verify>
  <done>Service tests pass and prove BUSINESS-only behavior, role rules, projected status, duplicate handling, and read-only effective EXPENSE transaction linking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User request to service | Role, workspace, and fiscal input are untrusted until validated. |
| TaxGuide to Transaction | TaxGuide may read/link an existing Transaction but must not mutate ledger state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-S5-015A-02-01 | Elevation of Privilege | markPaid/cancel | mitigate | Service rejects ACCOUNTANT/VIEWER for paid/cancel even if route wiring is wrong. |
| T-S5-015A-02-02 | Tampering | paidTransactionId | mitigate | Require same-workspace, `isPaid = true`, `status = COMPLETED`, and `type = EXPENSE`; never call Transaction mutation APIs. |
| T-S5-015A-02-03 | Information Disclosure | repository lookups | mitigate | Repository methods include `workspaceId` in all guide id lookups. |
| T-S5-015A-02-SC | Tampering | npm/pip/cargo installs | accept | No package installation is planned. |
</threat_model>

<verification>
<automated>cd backend; pnpm exec prisma validate</automated>
<automated>cd backend; pnpm exec tsc --noEmit</automated>
<automated>cd backend; pnpm test -- tests/services/TaxGuideService.test.ts</automated>
<automated>git diff --check</automated>
<automated>git diff -- frontend</automated>
</verification>

<success_criteria>
Plan 02 succeeds when service behavior satisfies TG-01, TG-02, TG-03, TG-05, TG-07, and TG-08 without routes, uploads, frontend changes, or Transaction mutation.
</success_criteria>

<output>
Create `.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-02-SUMMARY.md` when done.
</output>
