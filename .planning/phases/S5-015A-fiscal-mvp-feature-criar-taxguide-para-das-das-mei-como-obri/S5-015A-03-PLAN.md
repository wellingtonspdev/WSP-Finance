---
phase: S5-015A
plan: 03
type: tdd
wave: 3
depends_on:
  - S5-015A-02
files_modified:
  - backend/src/controllers/TaxGuideController.ts
  - backend/src/routes.ts
  - backend/tests/routes/TaxGuide.route.test.ts
autonomous: false
requirements:
  - TG-01
  - TG-02
  - TG-03
  - TG-04
  - TG-05
  - TG-07
  - TG-08
must_haves:
  truths:
    - "REST routes expose create/list/get/paid/cancel for workspace-scoped TaxGuides."
    - "OWNER and ACCOUNTANT can create guides in BUSINESS workspaces."
    - "ACCOUNTANT receives 403 for mark-paid/cancel and the service mutation is not called."
    - "VIEWER cannot mutate TaxGuide."
    - "Route responses expose projected `status` only, not stored/projected dual fields."
  artifacts:
    - path: "backend/src/controllers/TaxGuideController.ts"
      provides: "Zod-validated request handling and safe DTO responses"
    - path: "backend/src/routes.ts"
      provides: "Auth, workspace, and RBAC route wiring"
    - path: "backend/tests/routes/TaxGuide.route.test.ts"
      provides: "Route, RBAC, validation, and cross-tenant tests"
  key_links:
    - from: "backend/src/routes.ts"
      to: "backend/src/middlewares/WorkspaceMiddleware.ts"
      via: "TaxGuide route chain"
      pattern: "WorkspaceMiddleware"
    - from: "backend/src/routes.ts"
      to: "backend/src/middlewares/RbacMiddleware.ts"
      via: "ACCOUNTANT create/upload and OWNER paid/cancel gates"
      pattern: "RbacMiddleware"
    - from: "backend/src/controllers/TaxGuideController.ts"
      to: "backend/src/services/TaxGuideService.ts"
      via: "controller delegates parsed request to service"
      pattern: "TaxGuideService"
---

<objective>
Expose TaxGuide service behavior through backend REST routes with existing auth, workspace, RBAC, and zod validation patterns.

Purpose: make the fiscal obligation API usable by authorized backend clients while preserving tenant boundaries and role-specific mutation rules.
Output: controller, route registrations, and focused route tests.
</objective>

<execution_context>
@C:/Users/Wellington/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Wellington/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-CONTEXT.md
@.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-RESEARCH.md
@.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-VALIDATION.md
@backend/src/routes.ts
@backend/src/middlewares/AuthMiddleware.ts
@backend/src/middlewares/WorkspaceMiddleware.ts
@backend/src/middlewares/RbacMiddleware.ts

<interfaces>
Controller and route rules:
- Use `req.workspaceId`; never trust workspaceId from body.
- `POST /tax-guides`, `GET /tax-guides`, and `GET /tax-guides/:id` must run through AuthMiddleware and WorkspaceMiddleware.
- Create permits OWNER and ACCOUNTANT. Paid/cancel are OWNER-only.
- `PATCH /tax-guides/:id/paid` accepts `paidTransactionId` and delegates effective same-workspace paid EXPENSE validation to the service from Plan 02.
- Response DTOs return projected `status` as the single public status field.
</interfaces>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | REST management for TaxGuide obligations. | Plan 03 |
| REQ | TG-01 Create DAS succeeds. | Plan 03 route |
| REQ | TG-02 Create DAS_MEI succeeds. | Plan 03 route |
| REQ | TG-03 PERSONAL workspace returns 403. | Plan 03 route + service |
| REQ | TG-04 Cross-tenant request returns 403/no data. | Plan 03 route |
| REQ | TG-05 ACCOUNTANT create but paid forbidden. | Plan 03 route |
| REQ | TG-07 OVERDUE status in responses. | Plan 03 DTO |
| REQ | TG-08 Transaction link respects workspace/effective payment. | Plan 03 paid route delegates |
| CONTEXT | D-02, D-06, D-07, D-08, D-09, D-10, D-11, D-15. | Plan 03 |
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED TaxGuide route and RBAC tests</name>
  <files>backend/tests/routes/TaxGuide.route.test.ts</files>
  <behavior>
    - OWNER and ACCOUNTANT receive 201 for valid BUSINESS create.
    - VIEWER create returns 403.
    - PERSONAL workspace create returns 403.
    - Missing auth returns 401; invalid workspace context follows existing middleware behavior.
    - Cross-tenant get/paid/cancel never returns another tenant guide.
    - List supports status, competenceMonth, and competenceYear filters.
    - OWNER mark-paid succeeds with valid paidTransactionId.
    - ACCOUNTANT paid/cancel returns 403 and service paid/cancel methods are not called.
    - Invalid body/query values return 400.
  </behavior>
  <action>Create route tests before controller/route implementation. Mock `TaxGuideService` where route/middleware behavior is the target. Tests must assert no frontend involvement and no service paid/cancel call for forbidden ACCOUNTANT/VIEWER requests.</action>
  <verify>
    <automated>cd backend; pnpm test -- tests/routes/TaxGuide.route.test.ts</automated>
  </verify>
  <done>RED route tests exist and fail because the controller/routes are not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TaxGuide controller and route wiring</name>
  <files>backend/src/controllers/TaxGuideController.ts, backend/src/routes.ts</files>
  <behavior>
    - Controller validates body/query/params with zod.
    - Controller passes `req.workspaceId`, authenticated user id, and role context to service.
    - Routes use existing AuthMiddleware, WorkspaceMiddleware, and RBAC middleware ordering.
    - OWNER-only paid/cancel routes do not permit ACCOUNTANT.
  </behavior>
  <action>Create `TaxGuideController` with create/list/get/markPaid/cancel handlers. Register `GET /tax-guides`, `POST /tax-guides`, `GET /tax-guides/:id`, `PATCH /tax-guides/:id/paid`, and `PATCH /tax-guides/:id/cancel` in `backend/src/routes.ts` near related financial/workspace routes. Keep upload endpoints out of this plan. Do not add frontend files, new auth middleware behavior, or Transaction mutation shortcuts.</action>
  <verify>
    <automated>cd backend; pnpm exec tsc --noEmit</automated>
    <automated>cd backend; pnpm test -- tests/routes/TaxGuide.route.test.ts tests/services/TaxGuideService.test.ts</automated>
  </verify>
  <done>Routes are registered, route tests pass, forbidden role requests stop before service mutation, and projected `status` remains the only public status field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP client to Express route | Auth, workspace, role, params, body, and query are untrusted. |
| Controller to service | Controller must pass trusted workspace and caller context, not body workspace fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-S5-015A-03-01 | Elevation of Privilege | paid/cancel routes | mitigate | OWNER-only route gate plus route tests proving ACCOUNTANT does not call mutation service methods. |
| T-S5-015A-03-02 | Tampering | request body/query | mitigate | Zod validation for type, competence, amount, dueDate, status filters, and paidTransactionId. |
| T-S5-015A-03-03 | Information Disclosure | cross-tenant route access | mitigate | Auth + WorkspaceMiddleware + service-scoped lookups; route tests prove no other-tenant guide is returned. |
| T-S5-015A-03-SC | Tampering | npm/pip/cargo installs | accept | No package installation is planned. |
</threat_model>

<verification>
<automated>cd backend; pnpm exec tsc --noEmit</automated>
<automated>cd backend; pnpm test -- tests/routes/TaxGuide.route.test.ts tests/services/TaxGuideService.test.ts</automated>
<automated>git diff --check</automated>
<automated>git diff -- frontend</automated>
</verification>

<success_criteria>
Plan 03 succeeds when REST create/list/get/paid/cancel routes are protected, validated, and covered without uploads, frontend changes, or Transaction mutation.
</success_criteria>

<output>
Create `.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-03-SUMMARY.md` when done.
</output>
