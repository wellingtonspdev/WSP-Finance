---
phase: S5-015A
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/<timestamp>_add_tax_guide/migration.sql
  - backend/tests/integration/TaxGuideRLS.integration.test.ts
autonomous: false
requirements:
  - TG-01
  - TG-02
  - TG-04
must_haves:
  truths:
    - "TaxGuide persistence exists for DAS and DAS_MEI obligations in BUSINESS workspaces."
    - "TaxGuide.workspaceId is Int, matching Workspace.id per D-01."
    - "TaxGuide competence is stored as required competenceMonth and competenceYear per D-03."
    - "Duplicate workspace/type/competence rows are blocked per D-04."
    - "TaxGuide rows are tenant isolated by RLS and cross-tenant reads do not expose data."
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "TaxGuide model, TaxGuideType enum, TaxGuideStatus enum, relations, indexes, unique constraint"
    - path: "backend/prisma/migrations/<timestamp>_add_tax_guide/migration.sql"
      provides: "Database table, indexes, uniqueness, foreign keys, and RLS policies"
    - path: "backend/tests/integration/TaxGuideRLS.integration.test.ts"
      provides: "DB-backed uniqueness and tenant isolation proof"
  key_links:
    - from: "backend/prisma/schema.prisma"
      to: "Workspace.id"
      via: "TaxGuide.workspaceId Int relation"
      pattern: "workspaceId\\s+Int"
    - from: "backend/prisma/schema.prisma"
      to: "Transaction.id"
      via: "TaxGuide.paidTransactionId String? relation"
      pattern: "paidTransactionId\\s+String\\?"
    - from: "backend/tests/integration/TaxGuideRLS.integration.test.ts"
      to: "backend/src/test/prisma-test-clients.ts"
      via: "applicationClient tenant isolation test"
      pattern: "applicationClient"
---

<objective>
Add the backend persistence and tenant-isolation surface for TaxGuide before service or route behavior exists.

Purpose: establish the database contract for DAS/DAS-MEI obligations while preserving existing Workspace, Transaction, Account, Telegram/OCR, export, and frontend behavior.
Output: Prisma schema changes, migration SQL, and RED/GREEN integration tests for uniqueness and RLS.
</objective>

<execution_context>
@C:/Users/Wellington/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Wellington/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-CONTEXT.md
@.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-RESEARCH.md
@.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@backend/prisma/schema.prisma
@backend/src/test/prisma-test-clients.ts

<interfaces>
Existing schema facts to preserve:
- `Workspace.id` is `Int`; per D-01, `TaxGuide.workspaceId` must also be `Int`.
- `Transaction.id` is `String @id @default(uuid())`; per D-06/D-07, optional `TaxGuide.paidTransactionId` must be `String?`.
- `TransactionType` already includes `EXPENSE`; later payment validation must require linked payment transactions to be same-workspace, effective paid, and `type = EXPENSE`.
</interfaces>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Create TaxGuide as financial obligation for BUSINESS workspace. | Plan 01 schema/RLS; Plan 02 service; Plan 03 routes |
| REQ | TG-01 Create DAS succeeds. | Plan 01 enum/model; Plan 02 service; Plan 03 route |
| REQ | TG-02 Create DAS_MEI succeeds. | Plan 01 enum/model; Plan 02 service; Plan 03 route |
| REQ | TG-04 Cross-tenant request returns 403/no data. | Plan 01 RLS proof; Plan 03 route |
| RESEARCH | Add separate TaxGuideType and TaxGuideStatus enums. | Plan 01 |
| CONTEXT | D-01, D-03, D-04, D-05, D-06, D-07, D-13. | Plan 01 |
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED TaxGuide schema and RLS tests</name>
  <files>backend/tests/integration/TaxGuideRLS.integration.test.ts</files>
  <behavior>
    - Duplicate workspace/type/competence rows are rejected.
    - Tenant B application client cannot read tenant A TaxGuide rows.
    - Fixtures use numeric workspaceId and string UUID paidTransactionId when present.
  </behavior>
  <action>Create the integration test before schema implementation. Use existing test helpers from `backend/src/test/prisma-test-clients.ts`; `managementClient` is allowed only for setup/teardown. The test should fail RED because the TaxGuide Prisma model/table do not exist yet. Do not add service, controller, routes, uploads, or frontend files in this plan.</action>
  <verify>
    <automated>cd backend; pnpm test -- tests/integration/TaxGuideRLS.integration.test.ts</automated>
  </verify>
  <done>RED test file exists and fails only because TaxGuide schema/RLS is missing, not because of syntax or helper misuse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TaxGuide Prisma model, relations, and migration</name>
  <files>backend/prisma/schema.prisma, backend/prisma/migrations/<timestamp>_add_tax_guide/migration.sql</files>
  <behavior>
    - `TaxGuideType` contains `DAS` and `DAS_MEI`.
    - `TaxGuideStatus` contains `PENDING`, `PAID`, `OVERDUE`, and `CANCELLED`, with default `PENDING`.
    - `@@unique([workspaceId, type, competenceMonth, competenceYear])` prevents duplicates.
    - RLS policies follow existing workspace-owned table patterns.
  </behavior>
  <action>Update `schema.prisma` with a workspace-owned `TaxGuide` model using `id String @id @default(uuid())`, `workspaceId Int`, required `competenceMonth`, `competenceYear`, `dueDate`, Decimal `amount`, nullable `guideFileObjectKey`, nullable `paymentProofObjectKey`, nullable `paidTransactionId String?`, `createdByUserId Int`, timestamps, and explicit relations to Workspace, Transaction, and User as needed. Create a migration that creates the table, indexes, uniqueness, foreign keys, and RLS enablement/policies by copying the established pattern from existing workspace-owned tables. Do not change Transaction semantics or any frontend file.</action>
  <verify>
    <automated>cd backend; pnpm exec prisma validate</automated>
    <automated>cd backend; pnpm test -- tests/integration/TaxGuideRLS.integration.test.ts</automated>
  </verify>
  <done>Prisma validates, migration has RLS policy surface, uniqueness is enforced, and cross-tenant application-client reads are blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Prisma application client to PostgreSQL | RLS must prevent cross-workspace TaxGuide disclosure or mutation. |
| Migration to existing schema | New TaxGuide objects must not alter existing Transaction, Workspace, Account, Telegram/OCR, export, or frontend contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-S5-015A-01-01 | Information Disclosure | TaxGuide RLS policies | mitigate | Add DB-backed application-client test proving tenant B cannot read tenant A TaxGuide rows. |
| T-S5-015A-01-02 | Tampering | Competence uniqueness | mitigate | Enforce DB unique constraint for `workspaceId + type + competenceMonth + competenceYear`. |
| T-S5-015A-01-03 | Elevation of Privilege | Schema relation to Transaction | mitigate | Keep `paidTransactionId` optional and read-only for later service validation; no Transaction mutation in schema plan. |
| T-S5-015A-01-SC | Tampering | npm/pip/cargo installs | accept | No package installation is planned. |
</threat_model>

<verification>
<automated>cd backend; pnpm exec prisma validate</automated>
<automated>cd backend; pnpm test -- tests/integration/TaxGuideRLS.integration.test.ts</automated>
<automated>git diff --check</automated>
<automated>git diff -- frontend</automated>
</verification>

<success_criteria>
Plan 01 succeeds when TaxGuide schema, migration, uniqueness, and RLS proof exist, while frontend and unrelated financial/Telegram/export behavior remain untouched.
</success_criteria>

<output>
Create `.planning/phases/S5-015A-fiscal-mvp-feature-criar-taxguide-para-das-das-mei-como-obri/S5-015A-01-SUMMARY.md` when done.
</output>
