# External Integrations

**Analysis Date:** 2026-05-31

## APIs & External Services

**Brazilian Public Data:**
- BrasilAPI - CNPJ and CEP lookup fallback/source in `backend/src/infra/external/BrasilApiClient.ts`.
  - SDK/Client: native `fetch`
  - Auth: Not detected
- ReceitaWS - CNPJ lookup in `backend/src/infra/external/ReceitaWsClient.ts`.
  - SDK/Client: native `fetch`
  - Auth: Not detected
- ViaCEP - CEP lookup in `backend/src/infra/external/ViaCepClient.ts`.
  - SDK/Client: native `fetch`
  - Auth: Not detected
- External data API surface - exposed through `backend/src/controllers/ExternalDataController.ts` and routes `/external/document/:cnpj`, `/external/location/:cep` in `backend/src/routes.ts`.
  - SDK/Client: internal controller/service wrappers
  - Auth: Public route in `backend/src/routes.ts` with no `AuthMiddleware`

**Open Finance:**
- Open Finance webhook ingestion - receives bank movement batches and persists them as `BankMovement` through `backend/src/controllers/OpenFinanceWebhookController.ts`, `backend/src/services/OpenFinanceWebhookService.ts`, and `backend/src/services/FinancialIngestionEngine.ts`.
  - SDK/Client: incoming REST webhook, no external SDK
  - Auth: `OPEN_FINANCE_WEBHOOK_KEY` bearer token

**Telegram:**
- Telegram Bot API - optional polling bot for OCR/pairing flows in `backend/src/server.ts`, `backend/src/services/TelegramBotService.ts`, `backend/src/services/TelegramLinkService.ts`, `backend/src/services/TelegramLinkTokenService.ts`, `backend/src/services/TelegramContextResolver.ts`, `backend/src/services/TelegramOcrIngestionService.ts`, and `backend/src/services/TelegramOcrConfirmationService.ts`.
  - SDK/Client: `node-telegram-bot-api`
  - Auth: `TELEGRAM_BOT_ENABLED`, `TELEGRAM_BOT_TOKEN`
- Telegram web integration routes - authenticated pairing/status/destination management at `/integrations/telegram/*` in `backend/src/routes.ts` through `backend/src/controllers/TelegramIntegrationController.ts`.
  - SDK/Client: internal REST API consumed by frontend
  - Auth: WSP JWT plus Telegram link token/code tables in `backend/prisma/schema.prisma`

**Email:**
- Ethereal Email - development/test transactional email provider in `backend/src/providers/EtherealMailProvider.ts`.
  - SDK/Client: `nodemailer`
  - Auth: generated by `nodemailer.createTestAccount()`
- Transactional email use cases - account verification and password reset through `backend/src/services/VerificationService.ts`, `backend/src/services/PasswordResetService.ts`, and `backend/src/providers/IMailProvider.ts`.
  - SDK/Client: `IMailProvider` abstraction
  - Auth: provider-specific; only Ethereal provider detected in source

**Accounting Export:**
- Domínio TXT export - file layout and export generation through `backend/src/services/ExportService.ts`, `backend/src/services/ExportFormatter.ts`, `backend/src/services/ExportLayoutEngine.ts`, `backend/src/config/exportLayouts.ts`, and routes `/export/validate`, `/export/generate`, `/workspaces/:workspaceId/exports/:archiveId/download` in `backend/src/routes.ts`.
  - SDK/Client: file format generation, no external API client
  - Auth: WSP JWT + `WorkspaceMiddleware` + `RbacMiddleware('ACCOUNTANT')`

**AI Analysis:**
- AI insight provider abstraction - prompt-driven analysis interface in `backend/src/providers/AiProvider.ts` and deterministic implementation in `backend/src/providers/FakeAiProvider.ts`.
  - SDK/Client: Not detected; no external AI SDK dependency present in `backend/package.json`
  - Auth: Not applicable
- AI insight storage/API - internal generated insights stored in PostgreSQL through `backend/src/services/AiInsightService.ts` and exposed by `backend/src/controllers/AiInsightController.ts`.
  - SDK/Client: internal service
  - Auth: WSP JWT + workspace/RBAC middleware

## Data Storage

**Databases:**
- PostgreSQL
  - Connection: `DATABASE_URL`
  - Direct/admin connection: `DIRECT_URL`
  - Client: Prisma `@prisma/client` through `backend/src/lib/prisma.ts`
  - Schema: `backend/prisma/schema.prisma`
  - Migrations: `backend/prisma/migrations/` with 27 migration directories detected
  - CI service: `postgres:16-alpine` in `.github/workflows/ci.yml`
- PostgreSQL Row Level Security / tenant context
  - Connection: Prisma extended client in `backend/src/lib/prisma.ts`
  - Runtime context: `backend/src/lib/tenantContext.ts`
  - Middleware: `backend/src/middlewares/WorkspaceMiddleware.ts`
  - Current workspace injected with PostgreSQL `set_config('app.current_workspace_id', ...)` in `backend/src/lib/prisma.ts`

**File Storage:**
- Cloudflare R2 via S3-compatible API
  - Client: `@aws-sdk/client-s3` and `@aws-sdk/s3-request-presigner`
  - Implementation: `backend/src/providers/S3StorageProvider.ts`
  - Connection/auth: `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, `R2_PUBLIC_DEV_URL`
  - Certificate/customer-key encryption: `VAULT_MASTER_KEY`
- Local filesystem fallback
  - Implementation: `backend/src/providers/LocalStorageProvider.ts`
  - Selection: `E2E_STORAGE_PROVIDER=local` in `backend/src/providers/exportStorageProviderFactory.ts`
  - Local path: resolved `backend/uploads/`
- Static file serving
  - Route: `/files` in `backend/src/server.ts`
  - Upload route: `PUT /uploads/:filename` in `backend/src/routes.ts`

**Caching:**
- Database-backed accountant dashboard cache
  - Model: `AccountantDashboardCache` in `backend/prisma/schema.prisma`
  - Service/repository: `backend/src/services/AccountantCacheService.ts`, `backend/src/repositories/AccountantCacheRepository.ts`
  - Refresh endpoint: `/accountant/cache/refresh` in `backend/src/routes.ts`
  - Cron refresh: every 30 minutes in `backend/src/services/CronService.ts`
- In-memory caching dependency
  - Package: `node-cache` in `backend/package.json`
  - Specific runtime usage requires targeted service inspection before changing cache behavior.
- Frontend local storage cache/session items
  - Refresh token key: `wsp_refresh_token` in `frontend/src/shared/lib/axios.ts`
  - Auth/user/workspace state keys appear in `frontend/src/app/AuthProvider.tsx`, `frontend/src/shared/stores/useWorkspaceStore.ts`, and related frontend modules.

## Authentication & Identity

**Auth Provider:**
- Custom email/password auth
  - Implementation: `backend/src/controllers/AuthController.ts`, `backend/src/services/AuthService.ts`, `backend/src/config/authEnv.ts`, and `backend/src/middlewares/AuthMiddleware.ts`
  - Password hashing: `bcryptjs`
  - Access token: `jsonwebtoken` with `JWT_SECRET`
  - Refresh token persistence: `RefreshToken` model in `backend/prisma/schema.prisma`
  - Frontend API token handling: in-memory access token and `localStorage` refresh token through `frontend/src/shared/lib/axios.ts` and `frontend/src/app/AuthProvider.tsx`
- Workspace authorization / RBAC
  - Implementation: `backend/src/middlewares/WorkspaceMiddleware.ts`, `backend/src/middlewares/RbacMiddleware.ts`, `backend/src/middlewares/AdminMiddleware.ts`, `backend/src/middlewares/WorkspaceRouteParamGuard.ts`
  - Models: `Workspace`, `WorkspaceMember`, `WorkspaceInvite`, `WorkspaceRole`, and `SystemRole` in `backend/prisma/schema.prisma`
- Account verification and password reset
  - Models: `AccountVerificationToken` and `PasswordResetToken` in `backend/prisma/schema.prisma`
  - Routes: `/auth/verify`, `/auth/resend-verification`, `/password/forgot`, `/password/reset` in `backend/src/routes.ts`
  - Mail provider: `backend/src/providers/EtherealMailProvider.ts`

## Monitoring & Observability

**Error Tracking:**
- External error tracking service: None detected.
- Global backend error handling: `errorHandler` in `backend/src/server.ts`.
- RLS violation handling/auditing: Prisma error mapping and async audit log creation in `backend/src/server.ts`.

**Logs:**
- Backend logs use `console.log`, `console.warn`, and `console.error` in `backend/src/server.ts`, `backend/src/services/CronService.ts`, `backend/src/services/OutboxService.ts`, `backend/src/providers/S3StorageProvider.ts`, and `backend/src/providers/EtherealMailProvider.ts`.
- Audit logs are persisted in PostgreSQL through `AuditLog` model in `backend/prisma/schema.prisma` and `backend/src/services/AuditLogService.ts`.
- CI coverage artifacts are uploaded by `.github/workflows/ci.yml`.
- SonarCloud analysis is configured in `.github/workflows/ci.yml` and `sonar-project.properties`.

## CI/CD & Deployment

**Hosting:**
- Not detected. Repository contains application build/start scripts but no deployment target config was detected.
- Backend production start command is `node dist/src/server.js` from `backend/package.json`.
- Frontend production build command is `tsc -b && vite build` from `frontend/package.json`.

**CI Pipeline:**
- GitHub Actions
  - Main CI: `.github/workflows/ci.yml`
  - Release automation: `.github/workflows/release-please.yml`
  - Backend job: Node 20, pnpm 9, PostgreSQL 16 service, Prisma generate/migrate/seed, typecheck, Vitest coverage.
  - Frontend job: Node 20, pnpm 9, Vitest coverage.
  - E2E smoke job: Playwright Chromium against `frontend/tests/e2e/cacheLogin.spec.ts`.
  - Quality job: SonarCloud scan after coverage artifacts are downloaded.
- Release Please
  - Config: `release-please-config.json`, `.release-please-manifest.json`
  - Workflow: `.github/workflows/release-please.yml`

## Environment Configuration

**Required env vars:**
- `DATABASE_URL` - Prisma primary PostgreSQL connection in `backend/prisma/schema.prisma` and `backend/src/lib/prisma.ts`.
- `DIRECT_URL` - Prisma direct/admin connection in `backend/prisma/schema.prisma` and `backend/src/lib/prisma.ts`.
- `JWT_SECRET` - JWT signing secret enforced by `backend/src/config/authEnv.ts`.
- `FRONTEND_URL` - production CORS origin in `backend/src/server.ts`.
- `PORT` - backend listen port in `backend/src/server.ts`.
- `NODE_ENV` - production/test/development behavior in `backend/src/server.ts` and `backend/src/config/authEnv.ts`.
- `PRISMA_CONNECTION_LIMIT` - appended to `DATABASE_URL` by `backend/src/lib/prisma.ts`.
- `APP_URL` - local storage base URL in `backend/src/providers/LocalStorageProvider.ts`.
- `VITE_API_URL` - frontend API base URL in `frontend/src/config/env.ts`.
- `OPEN_FINANCE_WEBHOOK_KEY` - incoming Open Finance webhook bearer key in `backend/src/services/OpenFinanceWebhookService.ts`.
- `TELEGRAM_BOT_ENABLED` - toggles Telegram bot startup in `backend/src/server.ts`.
- `TELEGRAM_BOT_TOKEN` - Telegram Bot API token in `backend/src/server.ts`.
- `R2_ACCOUNT_ID` - Cloudflare R2 account in `backend/src/providers/S3StorageProvider.ts`.
- `R2_ACCESS_KEY_ID` - Cloudflare R2 access key id in `backend/src/providers/S3StorageProvider.ts`.
- `R2_SECRET_ACCESS_KEY` - Cloudflare R2 secret access key in `backend/src/providers/S3StorageProvider.ts`.
- `R2_BUCKET_NAME` - Cloudflare R2 bucket name in `backend/src/providers/S3StorageProvider.ts`.
- `R2_PUBLIC_DEV_URL` - Cloudflare R2 public/dev base URL in `backend/src/providers/S3StorageProvider.ts`.
- `VAULT_MASTER_KEY` - certificate vault SSE-C key material source in `backend/src/providers/S3StorageProvider.ts`.
- `E2E_STORAGE_PROVIDER` - local storage provider selector in `backend/src/providers/exportStorageProviderFactory.ts`.

**Secrets location:**
- `backend/.env` file present - contains environment configuration and was not read.
- `backend/.env.example` file present - template exists and was not read.
- `frontend/.env.example` file present - template exists and was not read.
- GitHub Actions uses repository secrets for `SONAR_TOKEN` in `.github/workflows/ci.yml` and `GITHUB_TOKEN` in `.github/workflows/release-please.yml`.

## Webhooks & Callbacks

**Incoming:**
- `POST /api/webhooks/open-finance` - Open Finance batch ingestion in `backend/src/routes.ts`, authorized by bearer token checked in `backend/src/services/OpenFinanceWebhookService.ts`.
- Telegram Bot API polling - started only when `TELEGRAM_BOT_ENABLED === 'true'` and `TELEGRAM_BOT_TOKEN` exists in `backend/src/server.ts`; this is polling, not an incoming HTTP webhook route.

**Outgoing:**
- Cloudflare R2/S3 presigned upload URLs - generated by `backend/src/providers/S3StorageProvider.ts` and exposed through `/uploads/presigned` in `backend/src/routes.ts`.
- Cloudflare R2/S3 presigned download URLs - generated by `backend/src/providers/S3StorageProvider.ts` and exposed through transaction attachment/export download routes in `backend/src/routes.ts`.
- Public API lookups to BrasilAPI, ReceitaWS, and ViaCEP - implemented in `backend/src/infra/external/*.ts`.
- Ethereal email delivery/preview - sent through `backend/src/providers/EtherealMailProvider.ts`.
- Telegram Bot API polling and messages - implemented in `backend/src/services/TelegramBotService.ts`.

---

*Integration audit: 2026-05-31*
